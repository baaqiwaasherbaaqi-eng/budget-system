# پروژه: سامانه بودجه شهرداری

## تاریخ آخرین آپدیت: 1405/06/31

## وضعیت: فاز ۱۸ (مستندسازی) + بازطراحی اطلاعات پایه (بخش ۲) — در حال انجام

## کارهای انجام شده:
- [x] فاز ۰: زیرساخت
- [x] فاز ۱: احراز هویت
- [x] فاز ۲: سال مالی
- [x] فاز ۳: طبقه‌بندی اقتصادی (نسخه قدیمی — بازطراحی شد)
- [x] فاز ۴: ساختار سازمانی (نسخه قدیمی — بازطراحی شد)
- [x] فاز ۵: بودجه پیشنهادی
- [x] فاز ۶: هوشمندسازی (کنار گذاشته شد)
- [x] فاز ۷: تایید و تصویب
- [x] فاز ۸: گزارشات پایه
- [x] فاز ۹: تخصیص و تامین اعتبار
- [x] فاز ۱۰: تناظر کدینگ (کنار گذاشته شد)
- [x] فاز ۱۱: مدیریت کاربران
- [x] فاز ۱۲: اصلاح بودجه
- [x] فاز ۱۳: گزارش پیشرفته
- [x] فاز ۱۴: تفریغ بودجه
- [x] فاز ۱۵: امنیت (Audit Log + Rate Limiting)
- [x] فاز ۱۶: UI/UX (داشبورد + نمودار + Dark mode)
- [x] فاز ۱۷: تست کامل
- [x] فاز ۱۸: مستندسازی (در حال انجام)
- [x] **بازطراحی بخش ۲ اطلاعات پایه: طبقه‌بندی سازمانی (درختی)**
- [x] **بازطراحی بخش ۴ اطلاعات پایه: طبقه‌بندی اقتصادی (درختی)**
- [x] **جدول `base_data` برای همه ساختارهای درختی**
- [x] **API کامل `base_data` (GET/POST/PUT/DELETE + tree + next-code)**
- [x] **UI درختی + جدولی برای سازمانی و اقتصادی (با سوئیچ و راست‌کلیک)**
- [x] **آپدیت `budget_proposals` با FK به `base_data`**
- [x] **آپدیت `budget-proposals.html` برای استفاده از `base_data`**
- [x] **حذف طبقه‌بندی سازمانی و اقتصادی از منوی داشبورد (فقط در اطلاعات پایه)**
- [x] **افزودن Footer مشترک به همه صفحات (لوگو + حق نشر «داده کاوان هوشمند»)**

## صفحات موجود:
- /login.html
- /dashboard.html
- /fiscal-years.html
- /economic-classifications.html (بازطراحی شده — درختی + تب منابع/مصارف)
- /organizations.html (بازطراحی شده — درختی)
- /budget-proposals.html (آپدیت شده)
- /reports.html
- /allocations.html
- /executions.html
- /users.html
- /revisions.html
- /advanced-reports.html
- /tafriq.html
- /audit-log.html
- /municipality-info.html
- /base-info.html

## کاربران پیش‌فرض:
- admin / Admin123! (مدیر سیستم)

## نقش‌ها:
- admin: دسترسی کامل
- manager: تایید بودجه
- expert: ثبت بودجه
- viewer: فقط مشاهده
- province: فقط گزارشات
- ministry: فقط گزارشات

## کارهای بعدی (طبق سند اطلاعات پایه):
- [ ] بخش ۳: برنامه راهبردی (چشم‌انداز، راهبرد، سیاست اجرایی)
- [ ] بخش ۴: طبقه‌بندی عملیاتی (مأموریت، برنامه، خدمت، فعالیت، طرح، پروژه)
- [ ] بخش ۵: حسابداری (دارایی‌ها، بدهی‌ها، سرمایه، عملکرد)
- [ ] بخش ۶: کالا و خدمات (کدینگ کالا، فهرست بها، استهلاک، احکام حقوقی)
- [ ] بخش ۷: اشخاص (کارکنان، حقیقی، حقوقی، تفصیلی شناور)

## ساختار `base_data`:
- `section`: 'organization', 'economic', 'operational', 'strategic', 'accounting', 'goods', 'persons'
- `type`: 'resource', 'expense', 'mission', 'program', 'general', ...
- `prefix`: 'س' برای سازمانی، NULL برای اقتصادی
- `code`: کد کامل (مثل `س-001001` یا `001008003`)
- `digit_count`: 2 تا 20 (پیش‌فرض 3)
- `level`: 0, 1, 2, ...
- `level_name`: خودکار (گروه، سرفصل، فصل، بند، جزء، حوزه، معاونت، اداره، واحد)
- `parent_id`: والد (nullable)
- `extra_data`: JSON (برای فیلدهای اضافی)
- `created_at_shamsi` / `updated_at_shamsi`: تاریخ شمسی خودکار

## قوانین کدینگ:
- هر بخش یک سرکد (کد مادر) دارد
- زیرکدها به کد مادر اضافه می‌شوند (مثال: `001` → `001001` → `001001001`)
- برای همه بخش‌ها (به‌جز بخش ۴/بودجه) یک حرف پیشوند قبل کد مادر قرار می‌گیرد (مثال: `س-001` برای سازمانی)
- تعداد ارقام توسط کاربر قابل تعریف است (۲ تا ۲۰، پیش‌فرض ۳)
- **بخش ۴ (بودجه):**
  - اقتصادی: منابع از `1` (یا `01` یا `001`)، مصارف از `2` (یا `02` یا `002`)
  - عملیاتی: مأموریت از `1`، برنامه زیرکدهای `1`، خدمت `00001` تا `49000`، فعالیت زیرکدهای `1`، طرح `49000` تا `99999`، پروژه زیرکدهای `1`

## مشکلات حل شده:
- ✅ ویرایش سال مالی
- ✅ حذف/غیرفعال کاربر (Foreign Key)
- ✅ فونت و اعداد نمودارها
- ✅ اعداد فارسی در URL
- ✅ ریدایرکت بعد از لاگین
- ✅ باگ پیشوند تکراری در `generateNextCode` (س-س-...)
- ✅ اتصال `budget_proposals` به `base_data`
- ✅ Footer مشترک بدون بهم ریختن layout

## یادداشت‌ها:
- برای deploy: `wrangler deploy`
- برای push: `git push origin main`
- برای migration remote: `wrangler d1 execute budget-db --file=... --remote`
- برای migration local: بدون `--remote`
- برای tail: `wrangler tail`
- برای حذف دیتا: `wrangler d1 execute budget-db --command="DELETE FROM ..." --remote`
- جداول قدیمی با `_old` suffix نگه داشته شده‌اند (برای بکاپ)
- دیتای تستی همه پاک شده — از صفر شروع می‌کنیم
- `toShamsi()` در `src/date.js` — تبدیل تاریخ میلادی به شمسی
- `generateNextCode()` در `src/base-data.js` — تولید کد خودکار
- فایل `public/footer.js` — Footer مشترک، توی `sidebar.js` لود می‌شه
- `login.html` — `footer.js` رو مستقیم لود می‌کنه (چون sidebar نداره)
- `Logo.png` توی `public/` قرار داره
- `/footer.js` و `/Logo.png` توی `staticPaths` توی `src/index.js` هستن